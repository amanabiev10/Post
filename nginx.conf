server {
    listen 80;
    server_name localhost;

    # Frontend-Konfiguration
    location / {
        proxy_pass http://frontend:5173/;  # Weiterleitung an den Vite-Frontend-Container
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Backend-Konfiguration
    location /api/ {
        proxy_pass http://backend:8000/;  # Weiterleitung an den Django-Backend-Container
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Optional: Konfiguration für HTTPS
# (Wenn du HTTPS benötigst, kannst du hier deine SSL-Zertifikate und Konfigurationen hinzufügen)
